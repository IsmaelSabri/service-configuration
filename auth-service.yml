admin-paths:

  paths:

    - uri: "/flats/save"

      method: "POST"

    - uri: "/flats/mensaje/{1}[1-9]+[0-9]*"

      method: "POST"

    - uri: "/flats/save/{1}[1-9]+[0-9]*"

      method: "POST"
